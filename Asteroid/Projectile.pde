class Projectile {
/*
  // Attributes ---------------------------------
  Vector pos;
  static Vector vel;
  Vector acc; //?
  static float absVel = 50;
  static int width = 3;
  static int height = 5;
  //color laserC = new Color(#FF0000);

  // Constructor --------------------------------
  Projectile(Vector gunPos, float alpha) {
    this.pos = new Vector(gunPos.getX(),gunPos.getY(),"c");
    this.vel = new Vector(absVel,alpha,"p");
  }

  // Methods ------------------------------------
  void show(laserC) {
    fill();//rgb - Rot
    noStroke();
    rectangle(this.pos.getX(),this.pos.getY(),width,height);
  }

  void update() {

  }

  boolean hit(Meteor) {


  }


  Vector getPos() {
    Vector position = this.pos;
    return position;
  }


  Vector getVel() {
    Vector velocity = this.vel;
    return velocity;
  }
*/
}
